# vite-plugin-pwa@1.2 hasn't yet loosened its peer range to include vite 8,
# even though it works fine at runtime. Keep npm tolerant of the mismatch so
# CI/Docker installs succeed the same way local installs do.
legacy-peer-deps=true
